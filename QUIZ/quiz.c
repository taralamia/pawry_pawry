#include<stdio.h>
int main()
{
float len,wid,area;

printf("Enter the value of len: ");
scanf("\n%f", &len);

printf("Enter the value of wid: ");
scanf("\n%f", &wid);

area= len * wid;

printf("area is %f", area);


return 0;

}
