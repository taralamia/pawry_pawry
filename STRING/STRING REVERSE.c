#include<stdio.h>
#include<string.h>
int main()
{
    char a[100];
    //int i,length;
printf("Enter the String: ");
    gets(a);

//for(i=0;a[i]!='\0';i++)

    //strcpy(b,a);
   strrev(a);


printf("Reversed String\n");
    puts(a);

}
